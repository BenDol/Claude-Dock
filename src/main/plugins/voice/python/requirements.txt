mcp>=1.0.0
sounddevice>=0.4.6
numpy>=1.24.0
faster-whisper>=1.0.0
pynput>=1.7.6
pyperclip>=1.8.0

# Optional: uncomment if you want the OpenAI Whisper API backend
# openai>=1.0.0

# Hotkey support matrix:
#   Windows       : full support
#   macOS         : requires Accessibility permission (prompted on first run)
#   Linux / X11   : full support (no root required, unlike the legacy `keyboard` lib)
#   Linux / Wayland: unsupported — use the /voice MCP slash command instead
